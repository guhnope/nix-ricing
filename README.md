This repo is really a test bed for trying new things in Nix. The two goals of the current config setup is
a) Compositor agnostic: so tools behave as much as possible regardless of which wayland compositor is used
b) easy theme switching, set in one line in configuration.nix

